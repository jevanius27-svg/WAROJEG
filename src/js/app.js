import '../styles/app.css'

window.addEventListener('DOMContentLoaded', () => {
  const btn = document.getElementById('submitBtn')

  btn.addEventListener('click', () => {
    alert('Pesanan Warojeg berhasil dikirim!')
  })
})
