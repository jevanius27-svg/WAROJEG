import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.warojeg.puspa',
  appName: 'Warojeg Puspa',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
